version: '3.0'

services:
  mysql:
    image: mysql:8.0
    container_name: data-sql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    environment:
      MYSQL_DATABASE: 'dataelementai'
      MYSQL_ROOT_PASSWORD: '123456'
      MYSQL_CHARSET: 'utf8mb4'
      MYSQL_COLLATION: 'utf8mb4_unicode_ci'
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  es:
    image: davyinsa/elasticsearch-ik:7.12.1
    container_name: data-es
    environment:
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
      - discovery.type=single-node
    volumes:
      - es-data:/usr/share/elasticsearch/data
    privileged: true
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -vq '\"status\":\"red\"'"]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    image: yexca/data-element:v1.3
    container_name: data-backend
    depends_on:
      mysql:
        condition: service_healthy
      es:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: always

  frontend:
    image: yexca/data-vue:v1.3
    container_name: data-frontend
    environment:
      - VUE_APP_BASE_URL=/api
      - VUE_APP_URL=http://backend:9852
    ports:
      - "8999:80"
    restart: always

volumes:
  mysql-data:
  es-data:
